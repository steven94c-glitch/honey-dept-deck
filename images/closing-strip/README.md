# closing-strip/

**Shows up:** the horizontal scrolling marquee right before the "Thank You." page (just after Chapter 03: Editing & Sound).

**Slots:** 8 images, named `01.jpg` through `08.jpg`. They scroll continuously and loop. Order matters — `01` is leftmost.

**Aspect:** wide (each frame renders at roughly 28vw × 32vh — closer to 16:10).

**Vibe:** echo / bookend to the intro strip. Different selection but the same documentary-cinematic register. Sends the deck out on tone.
